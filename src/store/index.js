import { createStore } from 'vuex'
import transportations from '@/store/modules/transportations';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    transportations,
  }
})
